apiVersion: v1
kind: Secret
metadata:
  name: personal-site-secrets
  namespace: personal
type: Opaque
stringData:
  face-api-password: "changeme"

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: personal-site-deployment-prod
  namespace: personal
  labels:
    app: personal-site-prod

spec:
  replicas: 1
  selector:
    matchLabels:
      app: personal-site-prod
  template:
    metadata:
      labels:
        app: personal-site-prod
    spec:
      containers:
        - name: personal-site-prod-container
          # if local
          # imagePullPolicy: Never
          # image: personal_site:latest
          # if remote
          imagePullPolicy: Always
          image: ghcr.io/prestonblackburn/personal_site/personal_site:main
          ports:
            - containerPort: 8000

          env:
            - name: FACE_API_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: personal-site-secrets
                  key: face-api-password
              
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "4096Mi"
              cpu: "4"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: personal-site-deployment-dev
  namespace: personal
  labels:
    app: personal-site-dev

spec:
  replicas: 1
  selector:
    matchLabels:
      app: personal-site-dev
  template:
    metadata:
      labels:
        app: personal-site-dev
    spec:
      containers:
        - name: personal-site-dev-container
          # if local
          # imagePullPolicy: Never
          # image: personal_site:latest
          # if remote
          imagePullPolicy: Always
          image: ghcr.io/prestonblackburn/personal_site/personal_site:dev
          args:
            ["uvicorn", "app.host:app", "--host", "0.0.0.0", "--port", "8001"]
          ports:
            - containerPort: 8001
          env:
            - name: FACE_API_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: personal-site-secrets
                  key: face-api-password
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "2048Mi"
              cpu: "2"

---
apiVersion: v1
kind: Service
metadata:
  name: personal-site-prod
  namespace: personal
spec:
  type: ClusterIP
  selector:
    app: personal-site-prod
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000 

---
apiVersion: v1
kind: Service
metadata:
  name: personal-site-dev
  namespace: personal
spec:
  type: ClusterIP
  selector:
    app: personal-site-dev
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001

